# Neon PostgreSQL — copy this to .env.local and fill in your values
# Also add DATABASE_URL to:
#   - Vercel dashboard > Settings > Environment Variables
#   - GitHub repo > Settings > Secrets > DATABASE_URL
DATABASE_URL=postgresql://neondb_owner:<password>@<host>/neondb?sslmode=require&channel_binding=require
